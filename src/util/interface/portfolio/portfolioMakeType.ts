export interface InputIsNullType {
  isNull: boolean;
}

export interface inputDataArrType {
  name: string;
  content: string;
}

export interface imageListType {
  isInFile: boolean;
  index: number;
}
